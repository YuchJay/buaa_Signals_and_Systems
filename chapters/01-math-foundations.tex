\chapter{数学预备知识}
\label{chap:math-foundations}

\section{复数、复指数与欧拉公式}
\label{sec:complex-numbers}

复数在信号与系统中不只是单纯的计算技巧。正弦信号可以写成复指数，系统的幅度变化和相位移动也能合在一次复数运算中。下面约定虚数单位为 $\iu$，即
\[
  \iu^2=-1.
\]
工程领域通常用 $\iu$（字母 $\mathrm{j}$）而不是 $\mathrm{i}$，以免与电流符号混淆。

\subsection{代数形式与复平面}

\begin{definition}[复数的代数形式]
  复数 $z$ 写成
  \[
    z=x+\iu y, \qquad x,y\in\R,
  \]
  其中 $x=\operatorname{Re}\{z\}$ 是实部，$y=\operatorname{Im}\{z\}$ 是虚部。两个复数相等，当且仅当\red{它们的实部和虚部分别相等}。
\end{definition}

复数 $z=x+\iu y$ 可以看作复平面上的点 $(x,y)$，横轴是实轴，纵轴是虚轴。这个几何视角把一个复数拆成两个量：它离原点有多远，以及它指向哪个方向。

\begin{definition}[模与幅角]
  复数 $z=x+\iu y$ 的模（magnitude）为
  \begin{equation}
    |z|=\sqrt{x^2+y^2}.
    \label{eq:complex-magnitude}
  \end{equation}
  当 $z\ne 0$ 时，满足
  \[
    x=|z|\cos\theta,\qquad y=|z|\sin\theta
  \]
  的角 $\theta$ 称为 $z$ 的幅角（argument）。若 $\theta$ 是一个幅角，则 $\theta+2k\pi$（$k\in\Z$）也是幅角。因此
  \[
    \arg z=\theta+2k\pi,\qquad k\in\Z.
  \]
  一般把区间 $(-\pi,\pi]$ 内的唯一幅角记作幅角主值 $\operatorname{Arg}z$。零的模为零，其\red{幅角没有定义}。
\end{definition}

由直角坐标求主幅角时，应根据 $(x,y)$ 所在象限判断角度。

程序中通常使用双参数函数 $\operatorname{atan2}(y,x)$；它比单独计算 $\arctan(y/x)$ 更可靠。

\begin{pitfall}[只用反正切求幅角]
  $\arctan(y/x)$ 不能区分相差 $\pi$ 的两个方向，并且在 $x=0$ 时无定义。例如 $1+\iu$ 与 $-1-\iu$ 的虚实部之比相同，主幅角却分别是 $\pi/4$ 和 $-3\pi/4$。\red{应当先判断象限}，程序中则直接使用 $\operatorname{atan2}(y,x)$。
\end{pitfall}

\subsection{共轭与基本运算}

\begin{definition}[复共轭]
  $z=x+\iu y$ 的复共轭（complex conjugate）为
  \[
    z^*=x-\iu y.
  \]
  共轭在复平面上\red{相当于关于实轴镜像}。$z^*$ 也可以写成 $\bar z$。
\end{definition}

常用关系如下：
\begin{align*}
  z+z^*&=2\operatorname{Re}\{z\},
  &z-z^*&=2\iu\operatorname{Im}\{z\},\\
  zz^*&=|z|^2,
  &\frac{1}{z}&=\frac{z^*}{|z|^2}\quad(z\ne0),\\
  (z_1z_2)^*&=z_1^*z_2^*,
  &|z_1z_2|&=|z_1|\,|z_2|.
\end{align*}
最后一式说明乘法会把两个模相乘。

幅角的变化则要借助极坐标形式来看。

\subsection{欧拉公式与极坐标形式}

欧拉公式（Euler's formula）把复指数和三角函数联系起来：
\begin{equation}
  \e^{\iu\theta}=\cos\theta+\iu\sin\theta.
  \label{eq:euler-formula}
\end{equation}
因此，任意非零复数都可以写成
\begin{equation}
  z=r\e^{\iu\theta},
  \qquad r=|z|>0,
  \qquad \theta\in\arg z.
  \label{eq:complex-polar-form}
\end{equation}
这称为复数的极坐标形式或指数形式。$r$ 是长度，$\e^{\iu\theta}$ 是单位圆上方向为 $\theta$ 的点。

\begin{intuition}
  乘上 $\e^{\iu\theta}$ 可以理解为在复平面上逆时针旋转 $\theta$，长度不变；再乘上正实数 $r$，则把长度放大为原来的 $r$ 倍。复数乘法于是变成了\red{“缩放再旋转”}。
\end{intuition}

若
\[
  z_1=r_1\e^{\iu\theta_1},\qquad
  z_2=r_2\e^{\iu\theta_2},
\]
则
\begin{align*}
  z_1z_2&=r_1r_2\e^{\iu(\theta_1+\theta_2)},\\
  \frac{z_1}{z_2}&=\frac{r_1}{r_2}\e^{\iu(\theta_1-\theta_2)}
  \qquad(z_2\ne0).
\end{align*}
也就是说，复数相乘时\red{模相乘、幅角相加}；相除时\red{模相除、幅角相减}。由于幅角只在模 $2\pi$ 的意义下确定，上面的角度关系也应按模 $2\pi$ 理解。

对整数 $n$，棣莫弗公式给出
\[
  \bigl(r\e^{\iu\theta}\bigr)^n
  =r^n\e^{\iu n\theta}
  =r^n\bigl(\cos n\theta+\iu\sin n\theta\bigr).
\]
反过来，对正整数 $n$，非零复数 $z=r\e^{\iu\theta}$ 的 $n$ 次方根共有 $n$ 个：
\begin{equation}
  z_k=r^{1/n}\e^{\iu(\theta+2k\pi)/n},
  \qquad k=0,1,\ldots,n-1.
  \label{eq:complex-nth-roots}
\end{equation}
这些根的模相同，幅角依次相差 $2\pi/n$，因而\red{均匀分布在同一个圆上}。后续分析系统函数的零点和极点时，这种结构会经常出现。

\begin{example}[代数形式与指数形式的转换]
  设 $z=-1+\iu\sqrt{3}$。由\cref{eq:complex-magnitude}可得 $|z|=2$。点 $(-1,\sqrt{3})$ 位于第二象限，所以
  \[
    \operatorname{Arg}z=\frac{2\pi}{3},
    \qquad
    z=2\e^{\iu 2\pi/3}.
  \]
  同一个复数也可写成 $2\e^{\iu(2\pi/3+2k\pi)}$，其中 $k\in\Z$。
\end{example}

令\cref{eq:euler-formula}中的角度分别取 $\theta$ 和 $-\theta$，可以得到信号分析中常用的两组关系：
\begin{align}
  \cos\theta
    &=\frac{\e^{\iu\theta}+\e^{-\iu\theta}}{2},
  \label{eq:cos-complex-exponential}\\
  \sin\theta
    &=\frac{\e^{\iu\theta}-\e^{-\iu\theta}}{2\iu}.
  \label{eq:sin-complex-exponential}
\end{align}
它们说明实正弦和余弦都可以分解成\red{一对旋转方向相反的复指数}。

\subsection{复指数与正弦信号}

对复数 $s=\sigma+\iu\omega$，复指数可以拆成
\begin{equation}
  \e^{st}=\e^{\sigma t}\e^{\iu\omega t}
  =\e^{\sigma t}\bigl(\cos\omega t+\iu\sin\omega t\bigr).
  \label{eq:complex-exponential-signal}
\end{equation}
其中 $\e^{\sigma t}$ 决定幅度包络，$\omega t$ 决定相位：$\sigma<0$ 时幅度衰减，$\sigma>0$ 时幅度增长，$\sigma=0$ 时信号以恒定模在复平面上旋转。若 $\omega\ne0$，纯复指数 $\e^{\iu\omega t}$ 的基本周期为
\[
  T_0=\frac{2\pi}{|\omega|}.
\]

带有初相位的实正弦信号也能写成复指数的实部。对 $A\geq0$，有
\begin{equation}
  A\cos(\omega t+\varphi)
  =\operatorname{Re}\!\left\{A\e^{\iu\varphi}\e^{\iu\omega t}\right\}.
  \label{eq:sinusoid-complex-form}
\end{equation}
复数 $A\e^{\iu\varphi}$ 同时保存振幅 $A$ 和初相位 $\varphi$。后续处理正弦稳态响应时，经常\red{先计算右侧的复指数，再取实部}。

离散时间复指数写作
\[
  x[n]=z^n=r^n\e^{\iu\omega n},\qquad z\ne0,\quad n\in\Z.
\]
$r$ 控制序列随 $n$ 的增长或衰减，$\omega$ 控制每前进一步所转过的角度。由于 $n$ 只能取整数，$\omega$ 与 $\omega+2k\pi$ \red{描述完全相同的序列}。纯复指数 $\e^{\iu\omega n}$ 为周期序列，当且仅当存在正整数 $N$ 和整数 $m$ 使
\[
  \omega N=2\pi m,
\]
也就是 $\omega/(2\pi)$ 为有理数。满足条件的最小正整数 $N$ 是基本周期。

\begin{pitfall}[照搬连续时间的周期公式]
  连续时间复指数在 $\omega\ne0$ 时总是周期信号，离散时间复指数却\red{不一定周期}。比如 $\e^{\iu n}$ 中 $\omega/(2\pi)=1/(2\pi)$ 不是有理数，因此不存在整数基本周期。
\end{pitfall}

\begin{remark}[相位只在模 $2\pi$ 下有意义]
  $\varphi$ 与 $\varphi+2k\pi$ 描述同一个方向，也给出同一个正弦信号。比较两个相位时，需要先说明采用的主值区间；相位在 $\pi$ 与 $-\pi$ 附近发生数值跳变，\red{并不表示信号突然改变}。
\end{remark}

\begin{pitfall}[混淆相位与时间平移]
  对 $\omega\ne0$，相位变化 $\varphi$ 可以换算为时间平移 $t_0=-\varphi/\omega$，因为
  \[
    \cos(\omega t+\varphi)=\cos\bigl(\omega(t-t_0)\bigr).
  \]
  同一个相位差在不同角频率下\red{对应的时间平移不同}，不能直接把“相差 $\varphi$”说成“延迟 $\varphi$”。
\end{pitfall}

\section{函数相等、几乎处处相等与广义函数}
\label{sec:function-equality-and-distributions}

在信号分析中，“两个函数相等”可能指不同层次的概念。若不先说明采用哪一种相等，单位阶跃在跳变点的取值、单位冲激的乘法等问题很容易混在一起。

\subsection{逐点相等与几乎处处相等}

\begin{definition}[逐点相等]
  若对定义域内每个 $t$ 都有 $f_1(t)=f_2(t)$，则称 $f_1$ 与 $f_2$ 逐点相等（pointwise equal）。只要存在一个点取值不同，它们就不是逐点相等的函数。
\end{definition}

\begin{definition}[几乎处处相等]
  若 $f_1$ 与 $f_2$ 只在一个零测集上取值不同，则称二者几乎处处相等（equal almost everywhere），记作
  \[
    f_1=f_2\quad\text{a.e.}
  \]
  有限集和可数集都是零测集，因此\red{有限个点的取值差异不影响几乎处处相等}。
\end{definition}

若一个无限集合能与自然数集 $\N$ 建立一一对应，就称它为可数无限集；否则称为不可数集。整数集、有限个或可数个冲激位置都可以逐项编号，而实数集 $\R$ 不可数。后文把冲激按其零点位置写成求和时，依赖的正是这种\red{可以逐项编号}的结构。

当两个函数可积且几乎处处相等时，它们的勒贝格积分相同。工程计算中最常见的例子是单位阶跃 $\stepfun(t)$：不同资料可能把 $\stepfun(0)$ 定义为 $0$、$1/2$ 或 $1$，这些版本并不逐点相等，但在普通区间积分中给出相同结果。因而只涉及普通积分时，\red{单个端点值通常可以忽略}。

\begin{pitfall}[把“几乎处处相等”理解成“处处相等”]
  几乎处处相等只保证积分意义下通常无法察觉零测集上的差异，并不允许在指定点随意代入。例如点值算子会直接读取 $f(0)$；单位冲激也会抽取冲激所在点的函数值。触发检查是：当前运算究竟是区间积分，还是包含点值、冲激抽样等\red{对单点敏感的操作}？
\end{pitfall}

\subsection{广义函数按积分作用相等}

单位冲激 $\impulse(t)$ 不是普通函数，而是广义函数（generalized function），更严格地说是分布（distribution）。对本课程而言，只需掌握它的判等方式：若两个广义函数 $F_1$ 与 $F_2$ 对每个足够光滑且支撑有界的测试函数 $\varphi$ 都满足
\begin{equation}
  \int_{-\infty}^{\infty}\varphi(t)F_1(t)\dd t
  =\int_{-\infty}^{\infty}\varphi(t)F_2(t)\dd t,
  \label{eq:distribution-equality}
\end{equation}
则认为 $F_1=F_2$。也就是说，广义函数不靠逐点函数值判等，而靠它对所有测试函数产生的\red{积分作用是否相同}判等。

\begin{remark}[这里需要多少勒贝格积分]
  本书只使用“零测集不改变积分”和“几乎处处相等”这两个结论，不展开测度构造与一般勒贝格积分理论。单位冲激的严格基础是分布理论；\cref{eq:distribution-equality} 提供后文验证冲激恒等式所需的最小工具。
\end{remark}

\section{内积与正交展开基础}
\label{sec:inner-product-foundations}

本节集中整理后续傅里叶级数、傅里叶变换及数据展开都会反复使用的数学工具。它们是跨章节的共同语言，因此不把它们局限在某一章的频域讨论中。

在复信号空间中，区间 $[a,b]$ 上的内积定义为
\begin{equation}
  \langle x,y\rangle
  =\int_a^b x(t)\overline{y(t)}\dd t,
  \label{eq:continuous-signal-inner-product}
\end{equation}
离散序列的对应定义为
\begin{equation}
  \langle x,y\rangle
  =\sum_{k=m}^{n}x[k]\overline{y[k]}.
  \label{eq:discrete-signal-inner-product}
\end{equation}
共轭放在第二个因子上，因而内积满足共轭对称性、对第一个因子的线性与可加性，以及正定性：
\begin{align*}
  \langle x,y\rangle&=\overline{\langle y,x\rangle},&
  \langle \lambda x,y\rangle&=\lambda\langle x,y\rangle,\\
  \langle x+z,y\rangle&=\langle x,y\rangle+\langle z,y\rangle,&
  \langle x,x\rangle&=\int_a^b|x(t)|^2\dd t\geq0.
\end{align*}
若 $\langle x,y\rangle=0$，称 $x,y$ 正交。复信号计算中必须保留共轭，否则 $\langle x,x\rangle$ 可能不是非负实数。

设函数族 $\{e_k(t)\}$ 两两正交，并且能够表示所考虑空间中的信号，则称其为正交基。展开
\begin{equation}
  x(t)=\sum_k a_k e_k(t)
  \label{eq:orthogonal-expansion}
\end{equation}
两边与 $e_r$ 取内积，得到投影系数
\begin{equation}
  \boxed{a_r=\frac{\langle x,e_r\rangle}{\langle e_r,e_r\rangle}}.
  \label{eq:orthogonal-projection-coefficient}
\end{equation}
正交归一基满足 $\langle e_r,e_r\rangle=1$，此时 $a_r=\langle x,e_r\rangle$。因此展开系数是信号沿各基方向的投影，而不是凭经验猜测的常数。

给定线性无关但不正交的函数 $\{\alpha_k\}$，Gram--Schmidt 方法递推构造
\begin{align}
  \beta_1&=\alpha_1,\\
  \beta_k&=\alpha_k-
  \sum_{m=1}^{k-1}
  \frac{\langle\alpha_k,\beta_m\rangle}
       {\langle\beta_m,\beta_m\rangle}\beta_m,
  \qquad k\geq2.
  \label{eq:gram-schmidt}
\end{align}
再令 $e_k=\beta_k/\sqrt{\langle\beta_k,\beta_k\rangle}$，即可得到正交归一基。后续章节只需把具体的傅里叶函数代入这套通用投影框架。

% 后续按资料进度补充“线性代数与正交展开视角”以及
% “微分方程、差分方程与收敛基础”；未成文前不生成空节标题。
